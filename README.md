# Pickleball Intelligence — Version 1.2.2 Single-File Build

This repair build places the complete application—HTML, styling, and JavaScript—inside one `index.html`.

## GitHub upload

Replace only the repository's existing `index.html` with this new one.

The other files may remain, but this build does not depend on them.

After the GitHub Pages deployment succeeds, open the live site and press:

- Mac: Command + Shift + R
- Windows: Ctrl + F5

The header badge should read `v1.2.2`.
